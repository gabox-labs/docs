---
title: "Overview"
description: "An onchain gacha built on one primitive: the backed position"
icon: "dice"
---

Gabox is an onchain gacha for NFTs and tokenized assets. Depositors list real assets in shared pools, buyers pay a fixed ticket price for a verifiably random draw, and every item drawn comes with a guaranteed instant buyback offer.

The whole protocol rests on a single primitive. When a depositor lists an item, they pair it with **backing**: an amount of USDC they lock alongside the asset. That one number is simultaneously the item's buyback bid, its draw weight, and the depositor's stake. Everything else, from ticket pricing to draw odds to buyback liquidity, is derived from it.

<Note>
Gabox is a working title. The protocol is built: the Solana program is complete and live on devnet, with real draws running against the MagicBlock VRF oracle. Mainnet follows external code review and a multisig upgrade authority. This documentation describes the mechanism as implemented.
</Note>

## Why this design matters

Most gacha and lootbox mechanics have a house that can lose. If the protocol promises buybacks from a shared vault, one jackpot cash-out can drain it. Gabox removes that failure mode entirely: every buyback is pre-funded by the depositor's own backing, posted before the item ever enters a pool.

| Property | How Gabox achieves it |
|---|---|
| No insolvency risk | Every possible payout is collateralized by depositor backing before the draw |
| No oracle risk | Prices derive from self-set backing, not floor price feeds |
| Verifiable fairness | Draws use onchain VRF with published odds |
| Guaranteed exit | Every drawn item can be sold back instantly for 90% of its backing |

## Scope

- **Assets**: NFTs and tokenized assets only (Collector Crypt cards and similar). No fungible tokens, because a liquid token has a known DEX price that breaks self-set backing. Illiquid uniques have fuzzy floors, so self-backing self-corrects.
- **Denomination**: USDC throughout. Backing, bids, tickets, and fees are all USD-denominated, so a SOL price move never reprices a standing bid.
- **Chain**: Solana.
- **Randomness**: MagicBlock VRF.

## Explore the docs

<Columns cols={2}>
  <Card title="How it works" icon="route" href="/how-it-works">
    The full loop: deposit, price, draw, keep or sell back.
  </Card>
  <Card title="Backed positions" icon="layer-group" href="/backed-positions">
    The one primitive everything is built on.
  </Card>
  <Card title="Pricing" icon="calculator" href="/pricing">
    Harmonic-mean pricing and a worked example.
  </Card>
  <Card title="The crown" icon="crown" href="/the-crown">
    The five-rank tithe board that pulls grails into pools.
  </Card>
</Columns>

See [Design evolution](/design-evolution) for how the design arrived at the backed-position mechanism.
