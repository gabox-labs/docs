# Gabox docs

Mintlify documentation for the Gabox program. Source behavior is checked against `../program/programs/gabox`, `../program/programs/gabox-hook`, and `../gabox-sdk`; deployment claims are checked against `../program/DEPLOYMENT.md`.

Run `mint dev` to preview and `mint broken-links` to check navigation and links. Edit page order in `docs.json`.
